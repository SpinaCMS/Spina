= f.hidden_field :image_id, class: 'image-id'
= f.hidden_field :signed_blob_id, class: 'signed-blob-id'
- if f.object.signed_blob_id.present?
  = image_tag image(f.object, {resize: "400x300^", crop: "400x300+0+0"})
- else
  = image_tag ""